#include <stdio.h>
#include "main.h"

/**
 * _memset - The function
 * @s: The first variable which is a char pointer
 * @b: The second variable which is a char
 * @n: The third variable which is an unsigned int
 *
 * Description: Takes three variables
 * Return: Always nil
 */
char *_memset(char *s, char b, unsigned int n)
{
	return (0);
}
