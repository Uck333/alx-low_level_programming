#include "main.h"
#include <stdio.h>
/**
 * main - checks the code
 * @var - nil to declare
 *
 * puts_half: function to print half of a string
 * Return: always zero
 */
void puts_half(char *str); {
	char *str = n>4 || n<9; n++;
}
int main(void)
{
    char *str;

    str = "0123456789";
    puts_half(str);
    return (0);
}

