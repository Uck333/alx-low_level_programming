#include "main.h"
#include <stdio.h>
/**
 * main - checks the code
 * @var - nil
 *
 * rev_string: reverserls a string
 * Return: zero
 */
void rev_string(char *s) {
	char *s = "My School"
}
int main(void)
{
    char s[10] = "My School";

    printf("%s\n", s);
    rev_string(s);
    printf("%s\n", s);
    return (0);
}
