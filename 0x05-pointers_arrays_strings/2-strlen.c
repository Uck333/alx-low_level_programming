#include "main.h"
#include <stdio.h>
/**
 * main - to check the code
 * @var - nil to declare
 *
 * strlen: returns the length of a string
 * Return: always nil
 */
int _strlen(char *s) {
	char *s = str;
}

