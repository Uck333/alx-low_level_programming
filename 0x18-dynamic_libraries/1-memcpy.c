#include <stdio.h>
#include "main.h"

/**
 * _memcpy - The function
 * @dest: The first variable which is a char pointer
 * @src: The second variable which is a char
 * @n: The third variable which is an unsigned int
 *
 * Description: Takes three variables
 * Return: Always nil
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{
	return (0);
}
