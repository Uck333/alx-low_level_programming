#include <stdio.h>
#include "main.h"

/**
 * _strspn - The function
 * @s: The first variable which is a char pointer
 * @accept: The second variable which is a char pointer
 *
 * Description: Takes two variables
 * Return: Always nil
 */
unsigned int _strspn(char *s, char *accept)
{
	return (0);
}
