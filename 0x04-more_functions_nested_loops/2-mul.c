#include "main.h"

/**
 * mul - function to check for uppercase
 * @a:variable int
 * @b:var2
 *
 * Description: checks uppercase
 * Return: 1 if uppercase 0 otherwise
 */

int mul(int a, int b)
{
	int sum;
	sum = (a * b);

	{
		return (sum);
	}
}
