/**
 * main - nil to declare
 * @void - nil
 *
 * Description: c program for alpha
 * Return: nil if successful
 */
#include <stdio.h>
int main(void)
{
	char i;
	for(i='a'; i<='z'; i++);
	putchar(i);
		return (0);
}
